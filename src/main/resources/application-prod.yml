spring:
  session:
    timeout: 86400 # session过期时间，一天失效

  # DataSource Config
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/ucenter # 线上数据库地址